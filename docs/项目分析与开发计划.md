# FlowNode 项目分析与开发计划

> 本文档记录 FlowNode 项目的整体分析、同类项目对比、技术栈优缺点，以及后续分阶段开发计划，供长期参考。

---

## 一、项目分析

**FlowNode 是一个基于 C# WinForms + .NET Framework 4.7.2 的可视化节点 / 流程图编辑器**，思路类似 Unreal Blueprint 或 Node-RED 的简化版。零 NuGet 依赖，纯 .NET BCL 实现。

### 1.1 技术栈一览

| 维度 | 内容 |
|------|------|
| 应用类型 | Windows 桌面应用 —— 可视化节点 / 流程图编辑器 |
| 语言 | C# |
| UI 框架 | Windows Forms（`System.Windows.Forms`） |
| 目标框架 | .NET Framework 4.7.2 |
| 输出类型 | `WinExe` |
| 构建工具 | MSBuild / Visual Studio 2019+（`.sln` + 旧式 `.csproj`） |
| 第三方库 | 无 NuGet 依赖，仅引用 BCL |
| 渲染 | GDI+ 自绘（`System.Drawing.Drawing2D` 贝塞尔曲线） |
| 序列化 | `System.Xml.Serialization.XmlSerializer`（XML） |
| 反射 | 节点自动发现与注册（`NodeFactory`） |

### 1.2 目录结构

```
FlowNode/
├── FlowNode.sln              # 解决方案（单项目）
├── README.md
├── LICENSE                   # MIT License (Copyright 2024 Lei Qian)
├── docs/                     # 文档（本文件所在）
└── FlowNode/                 # 主项目
    ├── Program.cs            # 应用入口
    ├── FlowNode.csproj
    ├── App.config            # .NET 4.7.2 运行时配置
    ├── Properties/
    ├── node/                 # 【核心】节点数据模型与执行引擎
    │   ├── Attribute/        # 节点注册特性
    │   ├── SystemNode/       # 内置系统节点
    │   │   └── FlowControl/  # 流程控制节点（Sequence/Branch/Loop）
    │   └── NodeBase / Pin / Connector / NodeManager / NodeFactory ...
    ├── app/
    │   ├── view/             # 【核心】编辑器 UI 与交互
    │   │   ├── control/      # 节点内嵌自定义 GDI+ 控件
    │   │   └── states/       # 编辑器状态机
    │   ├── command/          # 撤销 / 重做命令
    │   ├── serialization/    # XML 序列化
    │   └── node/             # 用户自定义函数节点（反射注册）
    ├── NodeSearchMenu.cs     # 节点搜索弹窗（右键空白搜索建节点）
    └── PropertyPanel.cs      # 属性面板（已接入 DemoForm 右侧）
```

### 1.3 架构分层（Model–View–Command）

```
node/            ← 数据模型 + 执行引擎（与 UI 解耦）
  NodeBase / Pin / Connector / NodeManager / NodeFactory
  SystemNode/    ← Sequence / Branch / Loop / Function 等内置节点

app/view/        ← 编辑器 UI 与交互
  NodeEditor.cs  ← 画布核心：网格、贝塞尔连线、缩放平移、HitTest、状态机调度
  NodeView.cs    ← 单个节点视图：绘制、引脚布局、内嵌控件
  states/        ← 状态机：Idle / Connecting / DraggingNode / Selecting / Panning
  control/       ← 节点内自定义 GDI+ 控件

app/command/     ← 命令模式（Undo / Redo）
app/serialization/ ← XML 序列化持久化
```

#### 核心类关系

- `NodeEditor` 用 `Dictionary<INode, NodeView>` 维护**数据 ↔ 视图**双向映射。
- `NodeManager` 持有图数据（节点、连接），并承担**执行引擎**（栈式调度）。
- `NodeView`（抽象）→ `DefaultNodeView` / `TestNodeView`，每个 `NodeView` 绑定一个 `NodeBase`。
- `NodeViewFactory` 负责"节点类型 → 视图类型"映射，启动时注册。
- `CommandManager` 维护撤销栈 / 重做栈，支持命令组。

#### 内置系统节点

| 节点 | 路径 | 功能 |
|------|------|------|
| `SequenceNode` | `/system/Sequence` | 顺序执行 |
| `BranchNode` | `/system/Branch` | 条件分支 True/False |
| `LoopNode` | `/system/Loop` | 循环（LoopBody / Completed） |
| `FunctionNode` | 动态 | 反射调用静态方法 |
| `GetObjectNode` / `SetObjectNode` | 动态 | 读写全局变量 |

#### 状态机交互

| 状态 | 触发 | 行为 |
|------|------|------|
| `IdleState` | 默认 | 点引脚→连接；点节点→拖拽；空白→框选；中键→平移；右键连线→删除 |
| `ConnectingState` | 拖拽引脚 | 绘制预览贝塞尔线，松手创建连接 |
| `DraggingNodeState` | 拖拽节点 | 移动选中节点，松手记录 Move 命令 |
| `SelectingState` | 空白左键拖拽 | 框选多个节点 |
| `PanningState` | 中键拖拽 | 平移画布 |

#### 执行引擎流程

1. `DemoForm` 点击 Execute → `NodeEditor.ExecuteFlow()` → `NodeManager.run()`
2. 找入口节点（无输入 Execute 连接的节点）
3. 栈式调度：`node.run(manager)` → `resolve()`（拉取上游数据）→ `excute()`（业务逻辑）→ `pushNextConnectNode()`（推进下游）

### 1.4 设计亮点

- **双轨 Pin 系统**：Execute（控制流）+ Data（数据流），与 Blueprint 一致。
- **状态机驱动交互**：鼠标交互拆成独立 State 类，`NodeEditor` 只做调度，扩展性好。
- **命令模式做撤销重做**：增删节点 / 连线 / 移动都封装成 Command，支持命令组。
- **反射注册节点**：`[SystemNode]` / `[Node]` / `[Function]` 特性 + `NodeFactory` 自动发现，新增节点成本低。
- **数据与视图解耦**：`node/` 逻辑层不依赖 UI，便于未来迁移 / 测试。

### 1.5 成熟度与遗留问题

核心编辑与执行闭环已可用，主要遗留：

- **A1 引脚内联编辑**（暂缓）：未连接数据输入引脚在节点上直接改值的方向保留，TextBox 叠加方案已否决，待更好交互。
- **DPI 适配**（暂缓）：自管 zoom/pan，需高分屏实测后再定 PerMonitor 方案。
- **脏区重绘**：视口裁剪已完成，按脏区增量重绘尚未做。
- **Undo 与 dirty**：Undo 回到已保存内容时标题栏仍可能显示 `*`（未做内容快照比对）。
- **全局变量值**：XML 保存节点与连线；变量**节点**已往返，但 `NodeManager` 内全局变量**取值**本身尚未单独序列化（需手动或通过 Set/Get 节点表达）。

已解决（勿再当作遗留）：Form1 死代码、PinType 隐患、AllocConsole、README/测试/CI、PropertyPanel 接入、变量节点复制粘贴与存盘等——详见附录执行记录。

### 1.6 代码规模

| 指标 | 数量 |
|------|------|
| C# 源文件 | ~58 个 |
| C# 代码行数（估算） | ~5,800–6,000 行 |
| 最大单文件 | `NodeEditor.cs` (~650 行) |

---

## 二、同类项目对比

### 同类完整产品 / 框架
- **Node-RED**（JS，可视化流程编排，IoT / 自动化领域事实标准）
- **Unreal Engine Blueprint**（本项目的设计原型）
- **Blender Geometry / Shader Nodes**、**Unity Visual Scripting (Bolt)**
- **ComfyUI**（AI 绘画节点工作流，近两年爆火）
- **n8n**（工作流自动化，Node-RED 的商业化竞品）

### 可直接复用的 .NET 节点编辑器库（重点参考）
- **Nodify**（WPF，最活跃的 .NET 节点编辑器控件库，社区质量高）
- **NodeNetwork**（WPF + ReactiveUI）
- **ST.Library.UI / NodeGraph**（WinForms 老牌节点控件）
- **NodeEditorWinforms**（WinForms，与本项目最接近）

### 其他生态的画布库
- Web：**React Flow / Rete.js / LiteGraph.js / Drawflow**
- C++：**imgui-node-editor**（Dear ImGui 生态）
- Qt：**NodeEditor (QtNodes)**

**结论**：该领域非常成熟，本项目本质是"自研 WinForms 版的 Blueprint"。作为学习 / 自研可控引擎有价值；若目标是快速产品化，Nodify(WPF) 或 Web 系（React Flow）能省掉大量底层渲染 / 交互工作。

---

## 三、技术栈优缺点（WinForms + .NET Framework 4.7.2 + GDI+ 自绘）

### 优点
- **上手快、所见即所得**：WinForms 设计器友好，桌面应用开发效率高。
- **零外部依赖**：纯 BCL，编译即跑，无供应链 / 版本问题。
- **GDI+ 自绘可控**：节点外观、连线、命中检测完全自掌控。
- **生态稳定、文档海量**：老技术，问题几乎都能搜到答案。
- **部署简单**：Windows 上单 exe 即可运行。

### 缺点（决定能否长期演进）
- **平台锁定**：.NET Framework 仅限 Windows，无法跨平台。
- **框架已停止演进**：4.8 是最后大版本，微软主推 .NET 5/6/7/8+，新特性与性能优化都在新平台。
- **GDI+ 性能瓶颈**：CPU 软件渲染，无硬件加速；节点上千、频繁缩放重绘时会卡。
- **高分屏（DPI）适配麻烦**：自绘需手动处理缩放，4K 屏易糊 / 错位。
- **无现代数据绑定 / MVVM**：状态同步靠手写事件，UI 复杂后维护成本上升。
- **UI 现代化天花板低**：动画、阴影、圆角、主题等做起来费劲，观感偏传统。

**一句话**：适合"快速做出能用的 Windows 工具 / 原型"，不适合"长期演进、跨平台、大规模高性能"的产品方向。

---

## 四、分阶段开发计划

### 阶段一：止血与基线（优先级最高）
1. **清理死代码**：删除 / 隔离 `Form1.cs` 这套旧 `NodeView`，避免概念混淆。
2. **修隐患**：`new PinType()` → 显式 `PinType.Execute`。
3. **去掉发布版控制台**：`AllocConsole()` 用 `#if DEBUG` 包裹。
4. **补 README**：怎么运行、怎么加自定义节点、XML 格式说明。

### 阶段二：补齐核心功能闭环
5. **接好节点搜索菜单**（`NodeSearchMenu`）：从 `NodeFactory` 反射结果填充，支持右键空白处搜索建节点。
6. **接入属性面板**（`PropertyPanel`）和**变量面板**（`VariableListControl`）到 `DemoForm`。
7. **完善文件菜单**：新建 / 打开 / 保存 / 另存，配合 XML 序列化。
8. **复制 / 粘贴、多选批量操作、对齐**等常规编辑能力。

### 阶段三：执行引擎健壮性
9. **图校验**：环检测、孤立节点、类型不匹配连线检查。（已完成：入口节点检查 + 执行流环检测 + 孤立节点/未连接数据输入引脚告警）
10. **执行可视化**：运行时高亮当前节点、断点、单步、日志面板。（已完成：当前节点高亮 + 日志面板 + 断点 + 单步/继续/停止）
11. **错误处理**：节点 `run()` 异常时不崩溃，能定位到具体节点。（已完成：异常被捕获记入日志，并把高亮以红色停在出错节点上）

### 阶段四：质量与性能（持续）
12. **渲染优化**：脏区重绘 / 视口裁剪（只画可见节点）。（已部分完成：视口裁剪——只绘制与可见区域相交的节点与连线；并修复 `DrawConnectors` 中因 `return` 导致一条连线缺失视图就中断后续全部连线绘制的 bug）
13. **DPI 适配**：高分屏测试。（暂缓：自绘画布已自管 zoom/pan，贸然开启 PerMonitorV2 可能与之叠加导致模糊/坐标错位，需在高分屏实测后再定方案）
14. **单元测试**：先覆盖 `node/`（纯逻辑层），再覆盖 Command 的 Undo/Redo。（已部分完成：新增 `FlowNode.Tests` 工程，覆盖 `PinTypeValidator`、`NodeManager`（连接校验/`Validate`/`ValidateWarnings`/数据对象）、`FunctionNode`（计算与数据沿连接传递）、`NodeFactory`（反射注册与创建）共 20 个用例，全部通过）
15. **CI**：GitHub Actions 做 build + test。（已完成：`.github/workflows/ci.yml` 在 `windows-latest` 上用 VS MSBuild 构建整个解决方案并运行测试 exe，自跑 runner 的退出码天然适配 CI；命令已在本地 Release 配置验证）

### 战略决策（投入阶段二前应明确长期方向）
- **坚持 Windows 桌面、长期维护** → 建议先把 `node/` 逻辑层迁到 .NET 8，UI 层逐步从 WinForms 迁到 **WPF + Nodify**。
- **跨平台 / 多人协作 / Web 化** → 逻辑层迁 .NET 8 做后端，前端换 **React Flow**，C# 当执行引擎。
- **学习 / 内部小工具** → 保持现状，按阶段一~三推进即可。

---

## 附录：执行记录

- **阶段一**（已完成）：清理 `Form1.*` 死代码、修复 `new PinType()` → `PinType.Execute`、`AllocConsole` 加 `#if DEBUG`、完善 README。
- **阶段二**（已完成）：
  - 接通节点搜索菜单 `NodeSearchMenu`：改为从 `NodeFactory.GetNodePath()` 拉取全部已注册节点路径，用 `NodeFactory.CreateNode(path)` 创建；区分屏幕坐标（菜单位置）与画布坐标（节点位置）；加入关键字过滤、回车/双击创建、失焦/Esc 关闭等弹窗交互。
  - 在 `IdleState` 右键空白处弹出搜索菜单，在鼠标所在画布位置建节点。
  - 接入属性面板 `PropertyPanel`：在 `DemoForm` 右侧停靠一个 `PropertyGrid`，选中单个节点时显示其属性，多选/空选时清空。
  - 在 `NodeEditor` 新增 `SelectionChanged` 事件，并在 `ClearSelection`/`AddToSelection`/`RemoveFromSelection` 及删除节点处触发；`DemoForm` 订阅该事件刷新属性面板（替代早期 `MouseUp`/`KeyUp` + `BeginInvoke` 的临时方案，更可靠且覆盖框选/删除/程序化操作）。
  - 变量面板未接入 `VariableListControl`：它与已接入的 `DataViewControl` 功能重复且实现粗糙，沿用 `DataViewControl` 管理全局变量。
  - 完善文件菜单：`New`（确认后清空图）/`Open`/`Save`/`Save As...`。新增 `NodeEditor.NewGraph()` 清空图（节点、连接、变量、选中、撤销历史），`DataViewControl.RefreshList()` 供新建/打开后刷新变量列表；`DemoForm` 跟踪 `currentFilePath` 实现 Save 直存、Save As 弹框，并在标题栏显示当前文件名。
  - 复制/粘贴与全选：`Ctrl+A` 全选，`Ctrl+C`/`Ctrl+V` 复制粘贴（含变量 Get/Set 节点元数据）；对齐/等距见 B1；Edit 菜单 Undo/Redo 与快捷键见 B6。
- **阶段三**（已完成）：
  - 图校验：`NodeManager.Validate()` 检查"图为空 / 无入口节点 / 执行流存在环"，`run()` 执行前先校验，未通过则抛错；`NodeEditor.ExecuteFlow()` 校验失败时在日志列出原因并中止。`run()` 增加步数上限（100 万）防止意外死循环卡死界面。环检测只针对执行引脚连接，循环节点靠运行时重压栈实现、不会被误报。
  - 执行可视化：`NodeManager` 新增 `NodeExecuting`/`Log` 事件，`run()` 每步触发；`NodeEditor` 执行时高亮当前节点（绿色发光边框）并按 `ExecutionStepDelayMs`（默认 200ms）单步停顿，执行结束/异常后清除高亮。
  - 日志面板：`DemoForm` 底部停靠只读多行 `TextBox`，订阅 `NodeEditor.ExecutionLog` 实时显示执行/校验/错误日志。
  - 单步执行与断点：把 `NodeManager` 执行引擎从"一次跑完"重构为可分步驱动（`BeginRun`/`Step`/`StopRun`/`PeekNext`/`IsRunning`/`HasMoreSteps`，`run()` 改为委托这些原语）。`NodeEditor` 提供 Run/Continue、Step、Stop，并在命中断点处暂停；`F9` 对选中的单个节点切换断点，断点节点左上角画红点。工具栏新增 Run/Continue、Step、Stop 三个按钮。执行节奏（高亮停顿）由 `RunLoop` 控制，单步则无停顿。
  - 收尾：`NodeManager.ValidateWarnings()` 输出告警（孤立节点、未连接且无默认值的数据输入引脚），执行前以 `[警告]` 记入日志但不阻断；节点执行抛异常时把高亮以**红色**停在出错节点上便于定位（`executionError` 标记，下次执行/停止/新建时清除）；`StopRun(silent)` 支持出错时静默复位；`NewGraph` 一并清空断点与执行状态。
- **阶段四**（进行中）：
  - 渲染优化（视口裁剪）：新增 `NodeEditor.GetVisibleWorldRect()`，`DrawNodes`/`DrawConnectors` 仅绘制与可见区域相交的节点和连线，缓解大图缩放/平移时的卡顿；顺带修复 `DrawConnectors` 中因 `return` 导致某条连线缺失视图即中断后续全部连线绘制的 bug（改为 `continue`）。
  - 单元测试：新增 `FlowNode.Tests`（.NET Framework 4.7.2）工程，用 NUnit 的 `[Test]`/`Assert` 编写、覆盖 `node/` 纯逻辑层共 20 个用例。因本机 NUnit3 引擎在枚举 .NET 7 运行时目录时崩溃（已知 bug），改用工程内置轻量 runner（`TestRunner.cs`）反射自跑、以退出码报告结果；待缓存具备修复版 `NUnit3TestAdapter(>=4.3)` 后加回 `Microsoft.NET.Test.Sdk`+适配器即可直接接入 VS Test Explorer/`dotnet test`，用例无需改动。提供 `scripts/run-tests.ps1` 一键构建并运行（自动把 `NUGET_PACKAGES` 指回真实目录以离线还原、用 VS MSBuild 处理 .resx）。测试工程已加入 `FlowNode.sln`。
  - CI：新增 `.github/workflows/ci.yml`，在 `windows-latest` 上 checkout → setup-msbuild → 还原 → 构建解决方案 → 运行测试 exe；测试失败时退出码非零使流水线失败。CI 命令已在本地 Release 配置跑通验证。
- **手册测试修复**（阶段四期间）：
  - `BranchNode.Condition` 引脚漏写数据类型（用了不带类型的 `createPin` 重载，`dataType=null`），导致连接校验对任何源都判为不兼容、bool 无法连入。补 `typeof(bool)` 与默认值 `false`（默认值同时避免未连接时 `(bool)null` 崩溃）。新增 `BranchNodeTests` 3 个用例。
  - `LoopNode` 不可用：循环次数 `loopCount` 是公有**字段**，而属性面板（`PropertyGrid`）与序列化（`NodeSerializationService`）只识别公有**属性**，故无法在 UI 设置、也不参与保存。改为属性 `LoopCount`。同时修复 `Index` 兼作计数器且跑完不归零导致二次运行循环体被跳过的问题：改用独立 `currentIndex`，结束后归零，`Index` 引脚仅对外暴露当前迭代序号。新增 `LoopNodeTests` 3 个用例。
  - 补齐测试所需基础节点：
    - 常量节点 `Constant/Int`、`Constant/Float`、`Constant/Bool`、`Constant/String`（`ConstantNodeBase<T>`）：自动运行的纯数据源，值以公有属性 `Value` 暴露（可在属性面板编辑、可序列化），用于给函数/分支等提供任意输入。
    - 打印节点 `Debug/Print`：`Value` 引脚为 `object`，可接任意数据；执行时通过新增的 `INodeManager.WriteLog`/`NodeManager.WriteLog` 把值写入 GUI 日志面板（此前 `IntTest.showMessage` 只写 `Console`，应用内不可见）。
    - 删除 `Test.cs`（死代码：已注释的 `Main` 及 `DataNode`/`Print2Node`/旧 `PrintNode`）。其中遗留的 `FlowNode.PrintNode` 与新 `FlowNode.node.PrintNode` 同名，按 C# 解析规则（外围命名空间类型优先于 `using` 导入）会遮蔽新节点，是测试一度异常的根因。
    - 新增 `BasicNodesTests` 等共 7 个用例（合计 32 个全过）。
  - 比较/逻辑/运算节点（`app/node/ComparisonOperator.cs`）：
    - `Compare`：`greater`/`greaterOrEqual`/`less`/`lessOrEqual`/`equal`/`notEqual`（int→bool），可直接驱动 `Branch.Condition`。
    - `Logic`：`and`/`or`/`not`（bool→bool），用于组合条件。
    - `MathOperator` 扩展 `mul`/`div`/`mod`（`div`/`mod` 对除零返回 0 以避免执行中断）。
    - 新增 `ComparisonTests` 7 个用例（合计 39 个全过）。
- **持久化正确性（B4）**：修复引脚默认值序列化往返。此前反序列化恢复引脚数据的代码被注释掉、且直接把字符串赋给 `pin.data` 不做类型还原。抽出共享工具 `PinValueConverter`（`ConvertStringToValue`），按保存时记录的 `ValueTypeName`（回退到引脚声明类型）用 `InvariantCulture` 还原为正确类型（原始/字符串/枚举；ref/out 自动解包），无法解析时不覆盖原值；序列化侧 `DefaultValue` 改用 `InvariantCulture` 写出。新增 `SerializationTests`（节点/连线/引脚默认值往返、常量节点 `Value` 往返、还原后保持 int 类型并可正确计算）3 个用例，以及 `PinValueConverterTests` 4 个用例（合计 46 个全过）。
- **编辑器易用性（A1，暂缓）**：未连接数据输入引脚的内联编辑（节点上直接改 `pin.data`）方向保留，但当前基于原生 `TextBox` 叠加的 UI 实现暂不采用，待后续更好的交互方案再推进。
- **属性面板与函数节点（094b541）**：新增 `NodePropertySheet`，统一暴露节点公有属性与 Pin Inputs；`FunctionNode` 的 `add` 等参数可在属性面板编辑；常量节点 `Value` 与输出引脚双向同步。
- **复制/粘贴增强（a0c876a）**：`NodeSnapshotHelper` 捕获/恢复节点属性与引脚数据；粘贴时保留属性与默认值，不再丢失自定义字段。
- **节点库扩展（8eb67a9）**：新增 float/string 比较、类型转换节点（`stringToInt` 等），便于搭建更复杂的数据流。
- **轻量 UX（6d33d81）**：常量/注释节点在画布上显示 `GetDisplaySubtitle()` 副标题；`NodeSearchMenu` 改为 TreeView 分组浏览并支持关键字过滤。
- **Track A 测试与命令修复（251c2b7）**：新增 `CommandManagerTests`（Add/Remove 节点与连线、MoveNodeView Undo/Redo、Redo 栈在 Execute 后清空）与 `FlowControlExecutionTests`（Branch/Loop 执行路径）；修复 `RemoveConnectorDataCommand` Redo 在 Undo 后无法正确还原连线的问题（按 sourcePin/targetPin 查找而非缓存 Connector 引用）。
- **节点库第二轮（6c034e3）**：String 操作（`concat`/`length`/`substring`）、Math 扩展（`abs`/`min`/`max`/`clamp`/`lerp`）、更多转换节点、`Debug/Delay`、`Comment` 节点。
- **多选布局（B1）**：`NodeEditorLayout` 提供左/右/上/下对齐与水平/垂直等距（≥3 节点）计算；`NodeEditor.AlignSelection` / `DistributeSelection*` 经 `MoveNodeViewCommand` + 命令组单次 Undo；工具栏 Edit 菜单接入。
- **未保存提示（B3）**：`DemoForm` 跟踪 `isDirty`，标题栏显示 `*`；关窗 / 新建 / 打开前提示保存（Yes/No/Cancel）；保存/另存/加载/新建成功后清除 dirty。`CommandManager.AfterModify` 驱动 dirty 标记。
- **控制流副标题（D2）**：`LoopNode` 显示 `×{LoopCount}`，`BranchNode` 显示条件 `true`/`false`，`DelayNode` 显示 `{DelayMs}ms`。
- **变量节点复制粘贴（B2）**：`GetObject`/`SetObject` 经 `VarName`/`VarTypeName`/`VarIsSet` 元数据复制粘贴。
- **属性 Undo（B5）**：`SetNodePropertyCommand`/`SetPinDataCommand`；属性面板修改可撤销。
- **Edit 菜单补全（B6）**：Undo/Redo；对齐 `Ctrl+Shift+L/R/T/B`，等距 `Ctrl+Shift+H/J/V`。
- **等距修复**：节点重叠或间距过小时 `span<0` 改为 gap=0 紧凑排列，垂直等距不再无效。
- **变量节点存盘（P1）**：`NodeData` 增加变量元数据；`NodeRestoreHelper` 供加载/粘贴共用；Get/Set 节点 Save/Load 往返。
- **第二波 UX（C1/C2/C4/C5）**：
  - **C1**：`FunctionNode` 副标题显示方法名与输入引脚摘要（如 `add  a=3, b=5`）；Compare 等函数节点显示运算符名。
  - **C2**：连线时悬停不兼容引脚显示红色预览与原因文字；松手失败时写入日志 `[连线] …`（`PinTypeValidator.CanConnect` 统一校验）。
  - **C4**：File → Open Recent（最多 8 条，持久化到 `%AppData%/FlowNode/recent.txt`）。
  - **C5**：View → Zoom to Fit All（`Ctrl+0`）/ Zoom to Selection（`Ctrl+Shift+0`）。
- **单元测试**：截至当前共 **108** 个用例全过。
